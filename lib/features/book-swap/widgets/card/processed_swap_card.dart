import 'package:flutter/material.dart';

class ProcessedCard extends StatelessWidget {
  const ProcessedCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
